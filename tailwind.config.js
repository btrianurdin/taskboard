import { Config } from "tailwindcss";

/**
 * @type {Config}
 */
const config = {
  content: [],
  theme: {
    extend: {
      fontFamily: {
        "dm-sans": ["DM Sans", "sans-serif"],
      },
    },
  },
  plugins: [],
};

export default config;
